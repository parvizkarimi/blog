{% extends 'base.html.twig' %}

{% block title %}Register
{% endblock %}

{% block body %}
	{% for flash_error in app.flashes('verify_email_error') %}
		<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
	{% endfor %}
	<div class="container mt-5">

		<h1>Register</h1>

		{# {{ form_start(registrationForm) }}
						{{ form_widget(registrationForm) }}
						<input type="submit" class="btn btn-primary" value="input">
						<button type="submit" class="btn btn-primary">Button M'inscrire</button>
						{{ form_end(registrationForm ) }} #}


		{{ form_start(registrationForm) }}
		{{ form_row(registrationForm.firstname) }}
		{{ form_row(registrationForm.lastname) }}
		{{ form_row(registrationForm.email) }}
		{{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}
		{{ form_row(registrationForm.agreeTerms) }}
		{# <input type="submit" class="btn btn-primary" value="input M'inscrire"> #}
		<button type="submit" class="btn btn-primary" formnovalidate>Button M'inscrire</button>
		{{ form_end(registrationForm) }}


	</div>
{% endblock %}
